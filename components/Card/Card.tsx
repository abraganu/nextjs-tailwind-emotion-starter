import { CardBody, CardHeader, CardWrapper } from "./styles";

export default function Card() {
    return(
        <CardWrapper>
            <CardHeader>
                This is a card header
            </CardHeader>
            <CardBody>
                this is a card body
            </CardBody>
        </CardWrapper>
    ) 
}